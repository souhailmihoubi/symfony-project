<form method="post" action="{{ path('app_p_c_delete', {'id': p_c.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ p_c.id) }}">
    <button class="btn">Delete</button>
</form>
